<!DOCTYPE html>
<html lang="fr" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Bienvenue | Create Your Food{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🍳</text></svg>">

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        brand: {
                            50: '#f5f7ff',
                            100: '#ebf0fe',
                            600: '#4f46e5',
                            700: '#4338ca',
                        }
                    }
                }
            }
        }
    </script>

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        body { font-family: 'Inter', sans-serif; }

        /* Glassmorphism effect */
        .glass-nav {
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            background-color: rgba(255, 255, 255, 0.8);
        }
        .dark .glass-nav {
            background-color: rgba(17, 24, 39, 0.8);
        }
    </style>
</head>

<body class="h-full antialiased text-slate-900 dark:text-slate-100 bg-slate-50 dark:bg-slate-950 transition-colors duration-300">

<nav class="glass-nav sticky top-0 z-50 border-b border-slate-200 dark:border-slate-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">

            <div class="flex-shrink-0">
                <a href="{{ path('foods_list') }}" class="flex items-center gap-2.5 group">
                    <div class="bg-brand-600 p-2 rounded-xl group-hover:rotate-12 transition-transform duration-300 shadow-lg shadow-brand-600/20">
                        <span class="text-xl">🍳</span>
                    </div>
                    <span class="text-xl font-extrabold tracking-tight bg-clip-text text-transparent bg-gradient-to-r from-brand-600 to-indigo-500 dark:from-brand-100 dark:to-brand-400">
                            CreateYourFood
                        </span>
                </a>
            </div>

            {#<div class="hidden md:flex items-center gap-1">
                <a href="#" class="px-4 py-2 rounded-lg text-sm font-medium hover:bg-slate-100 dark:hover:bg-slate-800 transition">Recettes</a>
                <a href="#" class="px-4 py-2 rounded-lg text-sm font-medium hover:bg-slate-100 dark:hover:bg-slate-800 transition">Favoris</a>
            </div>#}

            <div class="flex items-center gap-3">
                {% if app.user %}
                    <div class="relative group">
                        <button class="flex items-center gap-2 p-1 pr-3 rounded-full hover:bg-slate-100 dark:hover:bg-slate-800 transition border border-transparent hover:border-slate-200 dark:hover:border-slate-700">
                            <div class="h-8 w-8 rounded-full bg-gradient-to-tr from-brand-600 to-indigo-400 flex items-center justify-center text-white text-xs font-bold shadow-inner">
                                {{ app.user.userIdentifier|slice(0, 1)|upper }}
                            </div>
                            <span class="text-sm font-semibold hidden sm:inline">{{ app.user.userIdentifier }}</span>
                            <svg class="w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                        </button>

                        <div class="absolute right-0 mt-2 w-56 origin-top-right bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl shadow-2xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 translate-y-2 group-hover:translate-y-0">
                            <div class="p-2">
                                <div class="px-4 py-2 text-xs font-semibold text-slate-400 uppercase tracking-wider">Mon Compte</div>
                                <a href="{{ path('app_fridge') }}" class="flex items-center gap-3 px-4 py-2.5 text-sm rounded-xl hover:bg-slate-50 dark:hover:bg-slate-800 transition">
                                    <span>👨‍🍳</span> Create
                                </a>
                                <a href="#" class="flex items-center gap-3 px-4 py-2.5 text-sm rounded-xl hover:bg-slate-50 dark:hover:bg-slate-800 transition">
                                    <span>👤</span> Profil
                                </a>
                                <a href="#" class="flex items-center gap-3 px-4 py-2.5 text-sm rounded-xl hover:bg-slate-50 dark:hover:bg-slate-800 transition">
                                    <span>❤️</span> Favoris
                                </a>
                                <div class="my-2 border-t border-slate-100 dark:border-slate-800"></div>
                                <a href="{{ path('app_logout') }}" class="flex items-center gap-3 px-4 py-2.5 text-sm text-red-600 dark:text-red-400 font-semibold rounded-xl hover:bg-red-50 dark:hover:bg-red-900/30 transition">
                                    <span>🚪</span> Déconnexion
                                </a>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <a href="{{ path('app_login') }}" class="inline-flex items-center justify-center px-5 py-2.5 text-sm font-bold text-white bg-brand-600 rounded-xl shadow-lg shadow-brand-600/30 hover:bg-brand-700 hover:-translate-y-0.5 transition-all active:scale-95">
                        Se connecter
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>

<main class="relative">
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-full max-w-4xl h-64 bg-brand-600/5 blur-[120px] -z-10 pointer-events-none"></div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {% block body %}{% endblock %}
    </div>
</main>

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</body>
</html>